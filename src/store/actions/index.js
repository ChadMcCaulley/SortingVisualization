const bubble = () => ({
  type: 'BUBBLE'
})

const heap = () => ({
  type: 'HEAP'
})

const merge = () => ({
  type: 'MERGE'
})

const quick = () => ({
  type: 'QUICK'
})

const randomize = () => ({
  type: 'RANDOMIZE'
})

export {
  bubble,
  heap,
  merge,
  quick,
  randomize
}